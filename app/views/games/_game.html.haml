.game-details
  %dl
    %dt When
    %dd= game_date(@game)
    %dd.muted= relative_date(@game.starts_at)

    %dt Where
    %dd= @game.location

    %dt= @game.upcoming? ? "Who’s Playing" : "Who Played"
    %dd= game_players(@game, true)

    %dt= @game.upcoming? ? "Who’s Definitely Out" : "Who Declined"
    %dd= game_players(@game, false)

.form-actions
  = link_to 'Edit', edit_game_path(@game), :class => 'btn'
  = link_to 'Delete', game_path(@game), :method => "delete", :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger'
