= render ModalLinkComponent.new(title: 'Reschedule this Game', url: reschedule_game_path(game), button_text: 'Reschedule Game') do
  %p
    For
    %strong= game.date

  .form-group
    = label_tag :message, 'Message'
    = text_area_tag :message, 'The game is back on!', rows: 5, class: 'form-control'
