<%= @body %>

When: <%= @game.date %>
Where: <%= @location.name %> (<%= @location.url %>)
URL: <%= game_url(@game, access_token: @player.access_token) %>

<%= @confirmation %>: <%= raw respond_game_url(@game, access_token: @player.access_token, playing: 'yes') %>

<%= @rejection %>: <%= raw respond_game_url(@game, access_token: @player.access_token, playing: 'no') %>
