= simple_format(@body)

%p
  %strong When:
  = @game.date
  %br
  %strong Where:
  = @game.location

%p
  = link_to @positive_response, respond_game_url(@game, access_token: @player.access_token, playing: 'yes')
  %span.separator /
  = link_to @negative_response, respond_game_url(@game, access_token: @player.access_token, playing: 'no'), class: 'text-secondary'

%p
  You can also
  = link_to 'see who’s playing', game_url(@game)
  or
  = link_to('unsubscribe', new_removal_url(access_token: @player.access_token)) + '.'
